const PrefilledBadge = () => (
  <span className="inline-flex items-center gap-1 text-[10px] font-medium text-primary/70 ml-1.5">
    <span className="w-1.5 h-1.5 rounded-full bg-primary/40" />
    From pre-qualification
  </span>
);

export default PrefilledBadge;
